# Installe le projet en mode éditable + toutes ses dépendances runtime.
# Source de vérité unique : pyproject.toml ([project.dependencies]).
# Ce fichier existe pour la compatibilité avec les outils qui cherchent
# spécifiquement un requirements.txt (ex. Streamlit Community Cloud).
-e .
